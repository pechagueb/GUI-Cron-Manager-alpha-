// preload.js
// Fix: The context bridge for 'electronAPI' is no longer needed as the API key
// is injected at build time. This file can be left empty or removed if no other
// main-renderer communication is required.
